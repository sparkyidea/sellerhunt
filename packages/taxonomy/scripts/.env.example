# Taxonomy scripts environment variables
# Copy this to scripts/.env and fill in your values

# Required for map-ebay-to-shopify.ts
OPENAI_API_KEY=

# Note: pull-ebay-categories.ts reads EBAY_CLIENT_ID and EBAY_CLIENT_SECRET
# from apps/api/.env — no need to set them here.
