# Tomorrow’s work — September 18, 2026

## 1. Fix or restructure the panels

- [ ] Fix the black background flash when the main panel unmounts.
- [ ] Restructure the preview-to-main transition so the preview visibly expands and becomes the main panel, with continuous content throughout the animation.
- [ ] Verify navigation, unmounting, and preview expansion without background flashes or content appearing late in a separate panel.

## 2. Build the eBay scanning configuration page — on hold

- [ ] Find the GitHub PR remembered as “Buffalo” (currently believed to be under review) and record its link. Exact PR and status need confirmation.
- [ ] Wait for that PR to merge before starting this task.
- [ ] Ask Claude to create the page design first.
- [ ] Implement the design and configuration functionality afterward.

## 3. Replace the hooks library and persist sidebar state

- [ ] Confirm the intended packages: “TS hook” likely means `usehooks-ts`, and Alibaba’s hooks likely means `ahooks`.
- [ ] Replace the existing hooks library with the intended Alibaba hooks library.
- [ ] Persist sidebar expanded/collapsed state across navigation between the app view and admin dashboard, so it does not reset to expanded.
- [ ] Choose local storage or session storage according to the desired persistence lifetime.
- [ ] Ensure the initial render uses the saved state without briefly showing the default expanded state.
- [ ] Verify both saved states on reload and when switching between app and admin views.

## 4. Implement the scan listing page design and calculator

- [ ] Implement the scan listing page design.
- [ ] Let the user provide any two of profit, cost, and selling price and calculate the third:
  - Cost + target profit → required selling price.
  - Cost + selling price → profit.
  - Target profit + selling price → maximum purchase cost.
- [ ] Define whether profit is an amount or percentage, and how fees, shipping, and other expenses affect the calculation before implementing the formulas.
- [ ] Verify all three calculation modes and invalid or incomplete inputs.

## 5. Expand Trigger.dev machine capacity

- [ ] Obtain additional Oracle Cloud accounts.
- [ ] Provision additional machines and add them to the self-hosted Trigger.dev infrastructure.
- [ ] Verify that the added machines can run scan workloads.

## 6. Adjust the A1 container warm start setting

- [ ] Adjust the container warm start count for the A1 machine; determine and record the intended count before changing it.
- [ ] Verify warm start behavior and resource usage after the adjustment.
